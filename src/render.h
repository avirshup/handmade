#ifndef RENDER_H
#define RENDER_H

#include "./common.h"

// typedef struct Pixel Pixel;
// typedef struct ScreenBuffer ScreenBuffer;
errcode paint_window(SDL_Window* window, int t);

#endif  // RENDER_H
